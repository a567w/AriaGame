

#if 0
class KsFog
{
	public:
		KsFog();
		KsFog( KsColor* pColor, KsBool linearFog, KsReal start, KsReal end, KsReal density, KsBool pixelFog, KsBool rangeFog );
	 
		virtual		~KsFog();
	
		m_fogColor   = color;
		m_linearFog  = linearFog;
		m_fogStart   = start;
		m_fogEnd     = end;
		m_fogDensity = density;
		m_pixelFog   = pixelFog;
		m_rangeFog   = rangeFog;
};
#endif